mult <- function(a,b) {
  a*b
}
a = 5.0
b = 2.0
resultado = mult(a,b)

# a virgula utilizada para separar a variável do valor é util para gerar dataframes

sprintf("a,%f", a)
sprintf("b,%f", b)
sprintf("a*b,%f",resultado)
